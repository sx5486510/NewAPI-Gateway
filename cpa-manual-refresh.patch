# ============================================================================
# CPA Manual Token Refresh Patch
# ============================================================================
# Patch-Version:   1.0.0
# Patch-Marker:    +gateway-manual-refresh
# Base-CPA:        v7.2.102 (github.com/router-for-me/CLIProxyAPI/v7)
# Generated:       2026-07-28
#
# Adds:
#   - POST /v0/management/auth-files/refresh   (manual token refresh)
#   - Manager.RefreshAuthForRequest()          (exported refresh method)
#   - buildinfo.PatchSuffix                     (version marker)
#
# Verify patch is active (any one of):
#   1. Startup banner: "CLIProxyAPI Version: <ver>+gateway-manual-refresh"
#   2. HTTP header on /v0/management/*: X-CPA-PATCH: +gateway-manual-refresh
#
# git apply ignores these leading comment lines (they precede the first
# "diff --git" hunk), so the patch stays applicable as-is.
# ============================================================================
diff --git a/cmd/server/main.go b/cmd/server/main.go
index 5df6342c..47654892 100644
--- a/cmd/server/main.go
+++ b/cmd/server/main.go
@@ -70,7 +70,7 @@ func shouldEnableExampleAPIKeySafeMode(cfg *config.Config, commandMode, tuiMode,
 // It parses command-line flags, loads configuration, and starts the appropriate
 // service based on the provided flags (login, codex-login, or server mode).
 func main() {
-	fmt.Printf("CLIProxyAPI Version: %s, Commit: %s, BuiltAt: %s\n", buildinfo.Version, buildinfo.Commit, buildinfo.BuildDate)
+	fmt.Printf("CLIProxyAPI Version: %s%s, Commit: %s, BuiltAt: %s\n", buildinfo.Version, buildinfo.PatchSuffix, buildinfo.Commit, buildinfo.BuildDate)
 
 	// Command-line flags to control the application's behavior.
 	var codexLogin bool
diff --git a/internal/api/handlers/management/auth_files.go b/internal/api/handlers/management/auth_files.go
index 6e4c3bde..d59af097 100644
--- a/internal/api/handlers/management/auth_files.go
+++ b/internal/api/handlers/management/auth_files.go
@@ -2800,6 +2800,123 @@ func (h *Handler) rollbackSavedTokenRecords(ctx context.Context, savedPaths []st
 	}
 }
 
+// RefreshAuthToken manually refreshes a single auth token's credentials.
+// POST /v0/management/auth-files/refresh
+// Body: {"filename": "xai-xxx.json"}
+func (h *Handler) RefreshAuthToken(c *gin.Context) {
+	if h.authManager == nil {
+		c.JSON(http.StatusServiceUnavailable, gin.H{
+			"success": false,
+			"message": "core auth manager unavailable",
+		})
+		return
+	}
+
+	var req struct {
+		Filename string `json:"filename" binding:"required"`
+	}
+
+	if err := c.ShouldBindJSON(&req); err != nil {
+		c.JSON(http.StatusBadRequest, gin.H{
+			"success": false,
+			"message": fmt.Sprintf("Invalid request: %v", err),
+		})
+		return
+	}
+
+	filename := strings.TrimSpace(req.Filename)
+	if filename == "" {
+		c.JSON(http.StatusBadRequest, gin.H{
+			"success": false,
+			"message": "filename is required",
+		})
+		return
+	}
+
+	// Find auth by filename
+	auths := h.authManager.List()
+	var targetAuth *coreauth.Auth
+	for _, auth := range auths {
+		if auth.FileName == filename {
+			targetAuth = auth
+			break
+		}
+	}
+
+	if targetAuth == nil {
+		c.JSON(http.StatusNotFound, gin.H{
+			"success": false,
+			"message": fmt.Sprintf("Auth file not found: %s", filename),
+		})
+		return
+	}
+
+	// Check if auth has refresh credentials
+	if targetAuth.Metadata == nil {
+		targetAuth.Metadata = make(map[string]any)
+	}
+
+	refreshToken, hasRefreshToken := targetAuth.Metadata["refresh_token"].(string)
+	if !hasRefreshToken || refreshToken == "" {
+		c.JSON(http.StatusBadRequest, gin.H{
+			"success": false,
+			"message": "This auth file does not have a refresh token",
+		})
+		return
+	}
+
+	// Get old expiration time for response
+	oldExpired := ""
+	if exp, ok := targetAuth.Metadata["expired"].(string); ok {
+		oldExpired = exp
+	}
+
+	// Trigger refresh via auth manager
+	ctx, cancel := context.WithTimeout(c.Request.Context(), 30*time.Second)
+	defer cancel()
+
+	log.Infof("manually refreshing token for %s (%s)", targetAuth.Provider, targetAuth.ID)
+
+	// Call the internal refresh method
+	refreshed, err := h.authManager.RefreshAuthForRequest(ctx, targetAuth.ID, "")
+	if err != nil {
+		log.WithError(err).Errorf("failed to refresh token for %s", filename)
+		c.JSON(http.StatusInternalServerError, gin.H{
+			"success": false,
+			"message": fmt.Sprintf("Token refresh failed: %v", err),
+		})
+		return
+	}
+
+	if refreshed == nil {
+		c.JSON(http.StatusInternalServerError, gin.H{
+			"success": false,
+			"message": "Token refresh returned nil",
+		})
+		return
+	}
+
+	// Get new expiration time
+	newExpired := ""
+	if refreshed.Metadata != nil {
+		if exp, ok := refreshed.Metadata["expired"].(string); ok {
+			newExpired = exp
+		}
+	}
+
+	log.Infof("successfully refreshed token for %s (%s)", refreshed.Provider, refreshed.ID)
+
+	c.JSON(http.StatusOK, gin.H{
+		"success": true,
+		"message": "Token refreshed successfully",
+		"data": gin.H{
+			"filename":    filename,
+			"old_expired": oldExpired,
+			"new_expired": newExpired,
+		},
+	})
+}
+
 // PopulateAuthContext extracts request info and adds it to the context
 func PopulateAuthContext(ctx context.Context, c *gin.Context) context.Context {
 	info := &coreauth.RequestInfo{
diff --git a/internal/api/handlers/management/handler.go b/internal/api/handlers/management/handler.go
index 78fd505d..482328d6 100644
--- a/internal/api/handlers/management/handler.go
+++ b/internal/api/handlers/management/handler.go
@@ -267,6 +267,7 @@ func (h *Handler) Middleware() gin.HandlerFunc {
 		c.Header("X-CPA-VERSION", buildinfo.Version)
 		c.Header("X-CPA-COMMIT", buildinfo.Commit)
 		c.Header("X-CPA-BUILD-DATE", buildinfo.BuildDate)
+		c.Header("X-CPA-PATCH", buildinfo.PatchSuffix)
 		c.Header("X-CPA-SUPPORT-PLUGIN", pluginhost.SupportPluginHeaderValue())
 
 		clientIP := c.ClientIP()
diff --git a/internal/api/server.go b/internal/api/server.go
index 7853c84b..8335952e 100644
--- a/internal/api/server.go
+++ b/internal/api/server.go
@@ -1013,6 +1013,7 @@ func (s *Server) registerManagementRoutes() {
 		mgmt.DELETE("/auth-files", s.mgmt.DeleteAuthFile)
 		mgmt.PATCH("/auth-files/status", s.mgmt.PatchAuthFileStatus)
 		mgmt.PATCH("/auth-files/fields", s.mgmt.PatchAuthFileFields)
+		mgmt.POST("/auth-files/refresh", s.mgmt.RefreshAuthToken)
 		mgmt.POST("/vertex/import", s.mgmt.ImportVertexCredential)
 
 		mgmt.GET("/anthropic-auth-url", s.mgmt.RequestAnthropicToken)
diff --git a/internal/buildinfo/buildinfo.go b/internal/buildinfo/buildinfo.go
index 0bdfaf8b..bc93d3bc 100644
--- a/internal/buildinfo/buildinfo.go
+++ b/internal/buildinfo/buildinfo.go
@@ -12,4 +12,8 @@ var (
 
 	// BuildDate records when the binary was built in UTC.
 	BuildDate = "unknown"
+
+	// PatchSuffix indicates custom patches applied to this build.
+	// Empty for upstream builds, non-empty for patched versions.
+	PatchSuffix = "+gateway-manual-refresh"
 )
diff --git a/sdk/cliproxy/auth/conductor.go b/sdk/cliproxy/auth/conductor.go
index 716a6645..0bff0b1d 100644
--- a/sdk/cliproxy/auth/conductor.go
+++ b/sdk/cliproxy/auth/conductor.go
@@ -6180,6 +6180,12 @@ func (m *Manager) refreshAuth(ctx context.Context, id string) {
 	_, _ = m.refreshAuthForRequest(ctx, id, "")
 }
 
+// RefreshAuthForRequest is the exported version for manual refresh operations.
+// It triggers a synchronous credential refresh for the given auth ID.
+func (m *Manager) RefreshAuthForRequest(ctx context.Context, id, failedAccessToken string) (*Auth, error) {
+	return m.refreshAuthForRequest(ctx, id, failedAccessToken)
+}
+
 // refreshAuthForRequest performs a synchronous credential refresh for the given auth.
 // failedAccessToken lets concurrent callers reuse a refresh that already replaced the
 // access token that produced the unauthorized response.
